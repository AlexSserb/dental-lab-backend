export const operationStyle = (isSelected) => {
    return {
        padding: 2,
        marginY: 1,
        border: 2,
        borderRadius: "8px",
        backgroundColor: isSelected ? "#dcf0fa" : "#FFFFFF",
    };
};

export const calendarContainer = {
    width: "70%",
    padding: 2,
};
